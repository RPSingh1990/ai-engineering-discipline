# Scenario Examples

These are compact governed-task examples. They exist to avoid pretending one password-reset case proves a whole operating model.

Each scenario shows a different class of AI-assisted engineering risk:

- auth trust failure
- frontend identity/state failure
- data freshness/provenance failure
- external action governance failure

Use these as starting points, not proof that the system is complete.
